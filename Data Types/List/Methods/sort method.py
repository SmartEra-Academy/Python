# Sort the list in ascending order (by default) 
# or in descending order if specified

numbers = [5, 2, 9, 1, 5, 6]
numbers.sort()
print(numbers) # by default -> reverse=False

numbers.sort(reverse=True)
print(numbers)